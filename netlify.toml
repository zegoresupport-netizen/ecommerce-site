[build]
  command = "npm run build"
  publish = "dist"

# Set VITE_API_BASE_URL in Netlify Environment Variables to your REAL Render backend URL,
# for example: https://mousim-shop-api.onrender.com

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
