[build]
  command = "npm run build"
  publish = "dist"

# Set VITE_API_BASE_URL in Netlify Environment Variables to your Render backend URL,
# for example: https://your-backend-service.onrender.com

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
